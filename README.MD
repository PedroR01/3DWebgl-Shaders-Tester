Pasos que tuve que hacer para iniciar este proyecto de 0:

1. Instalar proyecto Vite vanilla + threejs
2. Instalar plugins para GLSL en VsCode
3. Instalar plugin para vite con comando 'npm install vite-plugin-glsl --save-dev'
4. Crear vite.config.js y copiar el contenido.
